{
  "build": {
    "commands": {
      "predeploy": "git clone https://github.com/sherlock-project/sherlock.git /app/sherlock && pip install -r /app/sherlock/requirements.txt && git clone https://github.com/phend0/blackbird-osint.git /app/blackbird-osint && pip install -r /app/blackbird-osint/requirements.txt"
    }
  }
}
